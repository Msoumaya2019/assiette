<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Reseau : Open Food Facts (codes-barres, recherche de produits) et le
         service d'analyse d'image. Aucune de ces deux sources n'est interrogee
         en arriere-plan. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Permet de distinguer « hors ligne » d'une panne du service, et donc
         d'afficher un message utile plutot qu'une attente sans fin. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- Prise de vue du repas et lecture d'un code-barres. -->
    <uses-permission android:name="android.permission.CAMERA"/>

    <!-- Rappels de repas, a partir d'Android 13. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- Permet de reprogrammer les rappels apres un redemarrage du telephone.
         Sans cela, Android oublie les alarmes en attente et les rappels
         disparaissent silencieusement. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <!-- L'appareil photo reste facultatif : l'application doit pouvoir etre
         installee sur une tablette qui n'en possede pas, la recherche manuelle
         et la saisie a la main restant disponibles. -->
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false"/>

    <!-- Aucune permission d'acces aux photos n'est demandee : le selecteur
         d'images du systeme renvoie uniquement l'image choisie par
         l'utilisateur, sans donner acces a la galerie. -->

    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Reception des rappels programmes.
             Declares ici, et non par le greffon, pour rester explicite sur ce
             que l'application enregistre aupres du systeme.
             Le mode de planification retenu est « inexact » : aucune permission
             d'alarme exacte n'est demandee, ce qui evite la restriction Google
             Play sur SCHEDULE_EXACT_ALARM. Un rappel peut donc arriver avec
             quelques minutes de retard, ce qui est sans consequence ici. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false" />
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
